
Процедура ПередЗаписью(Отказ)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли; 
	
	Если ЭтоНовый() Тогда
		Наименование = СтрШаблон("%1 ; %2 ; %3", 
		ОбщегоНазначения.ЗначениеРеквизитаОбъекта(ОбъектНастройки, "Наименование"), ПериодДействия.Наименование, УидБонуснойПрограммы);	
	КонецЕсли;	
	
КонецПроцедуры
